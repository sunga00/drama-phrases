# 드라마 표현 추출기 — 설계 문서 v2 (Claude Code용)

넷플릭스 외국 드라마/애니 자막에서 일상 실용 표현을 AI로 선별하고,
발음 듣기와 개인 단어장까지 지원하는 Streamlit 앱.

## 0. 요구사항 요약

1. 입력: Language Reactor가 내보낸 자막 엑셀 파일 (사용자가 업로드)
2. Claude API로 "일상에서 자주 쓰는 실용 표현" 선별 + 학습 정보 부가
3. 선별된 표현 중 사용자가 어려워하는 것만 ⭐로 골라 **내 단어장**에 저장
4. 표현마다 **발음 듣기(▶️)** — 성조를 몰라도 정확한 발음 확인 가능
5. 언어: **중국어 먼저 완성** → 이후 일본어, 영어 추가 (확장 가능한 구조로 설계)

## 1. 확정된 입력 데이터 형식 (실제 파일로 검증 완료)

Language Reactor Excel 내보내기. 시트명 `Subtitles`, 4개 열:

| 열 이름 | 내용 | 예시 |
|---|---|---|
| Time | 자막 시각 | `20s`, `1:06`, `44:03` |
| Subtitle | 원어 대사 | `我来吧` |
| Human Translation | 한국어 번역 | `제가 할게요` |
| Transliteration | 음차 표기 (중국어=병음) | `wǒ  lái  ba` |

검증된 특성 (파서에서 반드시 처리):
- 에피소드 1개 ≈ 640행
- Time 형식 혼재: 1분 미만 `20s`, 이후 `M:SS`/`MM:SS` → 초 단위 정규화
- 한 셀에 두 화자 대사가 `-好 -好嘞` 형태로 합쳐진 행 존재
- `（祥胜班 饼）` 같은 전각 괄호 무대지문 행 → 필터링
- 병음 열의 이중 공백 → 단일 공백 정규화
- 일본어 자막의 경우 Transliteration 열이 로마자 또는 비어있을 수 있음 (Phase 6에서 검증)

## 2. 아키텍처

```
[Streamlit UI — app.py]
  ├─ 사이드바: 언어 선택(중국어 우선), 작품명/에피소드 입력
  ├─ 탭 1 "분석": 파일 업로더 → [분석 시작] → 결과 테이블/카드
  ├─ 탭 2 "내 단어장": 저장된 표현 조회/검색/삭제
  │
  ▼
[languages.py]  ← 다국어 확장의 핵심
  언어 프로필 dataclass:
    - code: "zh" | "ja" | "en"
    - 표기 이름, 음차 표기 명칭(병음/후리가나/없음)
    - TTS 보이스 ID (zh-CN-XiaoxiaoNeural / ja-JP-NanamiNeural / en-US-JennyNeural)
    - 선별 프롬프트의 언어별 규칙 (예: 중국어=사극체 제외, 일본어=경어 레벨 표기)
  → 새 언어 추가 = 프로필 1개 추가로 끝나는 구조를 목표로 할 것
  │
  ▼
[parser.py]
  pandas 파싱 + 위 1번 함정 처리 → [{time_s, text, ko, reading}, ...]
  │
  ▼
[analyzer.py]  ← Claude API (anthropic SDK, model: claude-sonnet-4-6)
  ├─ 100행 청크 분할 → 청크별 호출, JSON 응답 강제
  ├─ 병합 + 중복 제거
  └─ 응답 파싱 시 코드펜스 제거 방어 코드
  │
  ▼
[tts.py]  ← edge-tts (무료, pip install edge-tts)
  ├─ async로 mp3 생성: communicate = edge_tts.Communicate(text, voice)
  ├─ 캐시: audio_cache/{lang}/{md5(text)}.mp3 — 있으면 재생성 안 함
  └─ Streamlit에서는 asyncio.run()으로 호출
  │
  ▼
[vocab_db.py]  ← SQLite (vocab.db)
  테이블 1 analyzed: 분석 이력 (파일 해시, 작품명, 분석 일시) — 재분석 방지 캐시
  테이블 2 expressions: 분석에서 나온 전체 표현
    (id, lang, zh_text UNIQUE(lang,text), reading, ko, level,
     usage_note, example_orig, example_ko, source_title, source_time_s,
     seen_count, created_at)
  테이블 3 my_vocab: 사용자가 ⭐로 저장한 표현 (expression_id FK, starred_at, memo)
```

## 3. UI 동작 흐름 (탭 1 "분석")

1. xlsx 업로드 + 작품명/화수 입력 → [분석 시작]
2. 진행 표시: st.progress (청크 7개 기준)
3. 결과: 표현 카드 목록. 카드마다
   - 원문 / 병음 / 한국어 뜻 / 난이도 뱃지(기초·중급·고급)
   - usage_note + 응용 예문
   - ▶️ 발음 듣기 (st.audio, 최초 클릭 시 edge-tts 생성 후 캐시)
   - ⭐ 단어장에 저장 (토글)
   - 재등장 표현이면 "N번째 등장" 뱃지
4. 하단: CSV / Markdown 다운로드 버튼

## 4. Claude API 프롬프트 설계 (analyzer.py)

system prompt 요지 (중국어 프로필 기준):

```
너는 중국어 학습 콘텐츠 큐레이터다. 드라마 자막 대사 목록에서
'일상 대화에서 그대로 쓸 수 있는 실용 표현'만 선별한다.

선별 기준:
- 포함: 인사/리액션/요청/거절/감탄 등 범용 회화 표현 (예: 我来吧, 不急, 说不定啊)
- 제외: 사극체·시대 어휘, 고유명사, 줄거리 종속 문장, 욕설,
  사용 빈도 낮은 관용구
- 같은 표현의 변형(好/好嘞)은 대표형 1개만

각 표현 부가 정보:
- level: "기초"|"중급"|"고급" (HSK 1-2/3-4/5-6 대응)
- usage_note: 사용 상황 한 줄 (한국어)
- example: 일상 응용 예문 1개 (원어 + 한국어)

응답은 아래 JSON 배열만. 코드펜스 금지.
[{"text":"...","reading":"...","ko":"...","level":"...",
  "usage_note":"...","example_orig":"...","example_ko":"...",
  "source_time_s":66}]
```

user 메시지: `시간초|원어|한국어|음차` 라인 형식 청크.
언어별 규칙 부분은 languages.py 프로필에서 주입.

## 5. 기술 스택 / 환경

- Python 3.11+, venv
- `streamlit`, `pandas`, `openpyxl`, `anthropic`, `python-dotenv`, `edge-tts`
- API 키: `.env`의 `ANTHROPIC_API_KEY` (console.anthropic.com 크레딧)
- TTS는 무료(edge-tts) — API 크레딧 소모 없음
- 실행: `streamlit run app.py`
- .gitignore: .venv/, .env, vocab.db, audio_cache/, *.xlsx

## 6. 단계별 구현 계획

1. **Phase 1 — 파서** ✅ 완료 (635행 검증)
2. **Phase 2 — 분석기**: analyzer.py + CLI 테스트 스크립트로 선별 품질 확인
3. **Phase 3 — Streamlit MVP**: 업로드 → 분석 → 카드 목록 + 다운로드
4. **Phase 4 — 단어장**: SQLite 3테이블, ⭐ 저장, 단어장 탭, 파일 해시 캐시
5. **Phase 5 — TTS**: tts.py + 카드에 ▶️ 버튼 + mp3 캐시
6. **Phase 6 — 다국어**: 일본어 프로필 추가(실제 일본어 수출 파일로 형식 검증 필요),
   이후 영어 프로필. UI에 언어 선택 반영
7. **Phase 7 — 확장(선택)**: Anki 내보내기(TSV), 여러 에피소드 일괄 처리

## 7. Claude Code 다음 프롬프트 (Phase 1 완료 상태에서, 복사해서 사용)

```
설계 문서가 v2로 업데이트됐어. 드라마표현추출기_설계문서.md 를 다시 읽어줘.

바뀐 점: 다국어 확장 구조(languages.py), 개인 단어장(⭐ 저장),
edge-tts 발음 듣기가 추가됐어. 중국어를 먼저 완성하는 방침은 그대로야.

.env에 API 키는 넣어뒀어. Phase 2(분석기)를 진행해줘.
단, 새 아키텍처에 맞게 languages.py의 중국어 프로필을 먼저 만들고,
analyzer.py가 그 프로필을 사용하도록 해줘.
완성되면 '축옥 1화.xlsx' 앞 200행으로 CLI 테스트를 돌려서
선별된 표현 목록을 보여줘.
```

## 8. 주의사항

- 자막 저작권: 추출·정리 결과는 개인 학습용으로만 사용
- API 비용: 에피소드 1개 ≈ $0.2~0.3 (Sonnet). 파일 해시 캐시로 재분석 방지
- edge-tts는 네트워크 필요 (Microsoft 서버 사용). 오프라인 대비가 필요하면
  macOS `say` 폴백을 Phase 7에서 고려
- Language Reactor 파일명이 자동 생성이므로 작품명/화수는 사용자 입력으로 받기
- Streamlit에서 st.session_state로 분석 결과를 유지해야 ⭐ 클릭 시 재분석되지 않음 (rerun 주의)
